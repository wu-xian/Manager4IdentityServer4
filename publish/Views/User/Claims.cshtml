@*
    For more information on enabling MVC for empty projects, visit http://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}

@model IList<IdentityServer4_Manager.Model.Claim>

<div class="modal-header">
    <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
    <h4 class="modal-title" id="modalTitle">
        User Claims
    </h4>
</div>
<div id="modal-body" class="modal-body">
    <input id="txt_userId" type="hidden" value="@ViewBag.UserId" />
    <table id="userClaim" class="table table-bordered">
        <thead>
            <button type="button" onclick="addNewLineForClaim()" class="btn btn-default btn-block">Create Claim</button>
        </thead>
        <tr>
            <th>Operation</th>
            <th>ClaimType</th>
            <th>ClaimValue</th>
        </tr>
        @if (Model != null)
        {
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        <button onclick="deleteUserClaim('@ViewBag.UserId','@item.ClaimType','@item.ClaimValue')"><i class="fa fa-trash"></i></button>
                    </td>
                    <td>@item.ClaimType</td>
                    <td>@item.ClaimValue</td>
                </tr>
            }
        }
    </table>

</div>
<div class="modal-footer">
    <button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
</div>